import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import React from 'react'
const formControls = [
    { name: "House Name", field: "name", inputType: "text" },
    { name: "Email", field: "email", inputType: "email" },
    { name: "Phone Number", field: "phone", inputType: "tel" }
  ];
export default index
